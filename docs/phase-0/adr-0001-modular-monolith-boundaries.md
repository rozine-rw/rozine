# ADR-0001 — Laravel modular-monolith boundaries

**Status:** Accepted

**Date:** 2026-08-24

**Approved:** 2026-08-28 — Engineering, Aminu and Erastus

**Owners:** Engineering — Aminu and Erastus; Security — Aminu and Erastus; Product, Business, and internal Legal — Robert; Compliance, Audit Operations, and Finance/Risk — Kimani; independent test — a pool member who was not the sole author or an eligible external delegate; required external legal/regulatory authority remains separate

## Decision

Rozine remains one Laravel 13 deployment and one transactional data boundary. Inertia web routes and versioned mobile API routes are separate transports over the same application actions. They must not duplicate financial, authorization, underwriting, evidence, or secondary-market rules.

New bounded contexts use vertical modules with internal layers:

```text
App\Domain\<Context>          pure rules, values, states, and domain events
App\Application\<Context>     use cases, transactions, policies, and ports
App\Infrastructure\<Context>  Eloquent persistence and provider adapters
App\Http                       Inertia/API controllers, requests, and Resources
```

The transport contract is shared deliberately:

1. An Inertia controller and its `/api/v1` counterpart invoke the same application action.
2. The action returns an application result or authorized read model, never an Inertia response or vendor DTO.
3. One Eloquent API Resource shapes the result for both transports. The API returns that Resource directly; the Inertia adapter resolves the same Resource into props.
4. A Resource may shape deliberately loaded and authorized data, but may not calculate money, authorize, mutate state, call providers, or perform workflow transitions.
5. Provider implementations remain behind application-owned ports. SDK types do not enter Domain objects or Resource schemas.

Cross-context writes happen only through the target context's application action or declared port. Direct table/model writes across protected ledger, settlement, underwriting, marketplace, audit-evidence, or identity seams are prohibited.

## Initial executable rules

`tests/Architecture/ArchitectureTest.php` starts with rules that are true of the current code: PSR-4 casing, prohibited debug/termination helpers, transport naming/inheritance, Resource isolation from actions/infrastructure/calculators, and transport-independent support calculations. Module-specific rules are added with the first module rather than pretending absent namespaces are already enforced.

## Strict typing — D-76, approved 2026-09-10

Aminu approved the strict-types recommendation in this task. Every human-authored PHP file under `app/`, `bootstrap/`, `config/`, `database/`, `routes/`, and `tests/` must begin with `declare(strict_types=1);` after the PHP opening tag and optional comments. A later declaration must not disable or replace it.

The scope includes standalone configuration and route files, anonymous migrations, factories, seeders, Pest test files, and test support code as well as named classes. Blade templates (`*.blade.php`) and generated `bootstrap/cache/**` files are excluded. Vendor, build, and generated artifacts outside these six source roots are outside this convention. No human-authored file is exempt; any future exception must identify the exact file, reason, owner, approver, and expiry before the enforcement scope changes. A generated marker or directory name alone does not grant an exclusion.

`tests/Architecture/StrictTypesTest.php` combines Pest's namespace expectation with a recursive file scan and PHP syntax-tree inspection. It checks every approved directory, including hidden and newly added/untracked PHP files, and rejects missing/disabled declarations, comment or string imitations, late declarations, and redeclarations. These tests run in the normal non-TIA suite and the architecture command. The negative-control harness plants missing and disabled declarations plus hidden-file violations in every source root and requires the corresponding file diagnostic before removing each fixture.

Strict typing does not validate transport input, prohibit an explicit cast, or replace authorization and financial rules. Those remain application responsibilities, and the existing PHPStan and 100% coverage gates continue to apply. Existing behavior must pass before this conversion is accepted.

## Protected-module rule delivery gate

The timing is part of the existing accepted decision: the **first implementation PR** for each ledger, settlement, underwriting-publication, seal, or immutable-evidence module must freeze its actual namespaces and allowed entry points here, add architecture rules over those existing symbols, and demonstrate a controlled failure followed by a passing run in that same PR. The test must assert that its target symbols exist so an empty namespace cannot pass as protection. Renaming or moving a module updates its rule and evidence in the same change.

Aminu owns the server-side implementation and Erastus supplies the non-author review (reversed if Erastus authors that change). These are enforceable module-entry requirements, not claims that future modules are already implemented or tested. Phase 0 establishes the current-source rules and this delivery gate; it does not require building later-phase protected modules early. Their behavior, authorization, immutable-evidence and PostgreSQL concurrency tests remain required with their implementation.

## Exact legacy exceptions

### Phase 1 identity entry points — 2026-09-23

`App\Domain\Identity\RoleAccess`, `MembershipTransitions` and `ActiveRolePolicy` own role availability, the membership lifecycle and active-role decisions. The application entry points are `RegisterIdentity`, `GetIdentityContext`, `ResolveVerifiedPerson`, `ChangeMembership`, `SelectActiveRole`, `AuthorizeActiveRole` and `ConfigureIdentityOperator` under `App\Application\Identity`. `Contracts\IdentityRepository` and `Contracts\IdentityAccessStore` bind only in `AppServiceProvider` to the two `App\Infrastructure\Identity\EloquentIdentity*` adapters. Registration remains atomic and grants no verification or membership.

`Party`, `RoleMembership`, `VerifiedPersonIdentity`, `IdentityOperator` and `IdentityAuditEvent` may be referenced only by the identity adapters, model relationships and factories. The architecture suite asserts concrete identity targets and enforces that boundary. The `identity-boundary` negative control plants a forbidden application-layer Party write, requires that rule to fail, removes it and verifies a clean suite.

Identity administration requires a dedicated staff account with verified email, confirmed MFA and a current enabled operator record. A trusted deployment operator uses `php artisan identity:operator <user-id> --reason="..." --no-interaction` to grant this narrow capability, or adds `--revoke` to withdraw it. It cannot be granted through HTTP or registration. Granting refuses an account with verified identity, membership history, an organization Party or a shared Party; an unused registration Party can be detached with an audit record. Revoking access remains possible without valid MFA. This is the identity operator bootstrap, not the full Admin permission matrix or a live staff onboarding approval.

`ResolveVerifiedPerson` records the operator's attestation against reviewed external evidence: the namespace-qualified provider reference identifies the same person across logins, and a separate evidence reference and reason are mandatory. The provider reference is hashed for matching and is never exposed in public Resources. A PostgreSQL advisory lock plus unique identity/Party keys serialize resolution. Existing verified identity/membership histories are rejected for explicit reconciliation instead of being moved or merged. This action does not fetch documents, run an external KYC/KYB provider, validate corporate mandates or grant transaction eligibility.

Membership commands lock the operator and Party, recheck current authority, enforce allowed state transitions and Auditor exclusivity, compare the membership revision, and write the mutation and immutable audit receipt in the same transaction. `pending` and `suspended` roles retain the exclusivity constraint; `revoked` is terminal. Actor-scoped request UUIDs and canonical permitted-input hashes give one effect for retries and reject changed payloads. Audit entries retain actor, target, reason, before/after, request identity, policy version and server time. PostgreSQL rejects updates/deletes of audit entries. The actor ID is historical data without a cascading user foreign key, so account deletion cannot rewrite the audit event.

Selection stores a membership ID and revision plus a monotonically increasing context revision on the authenticated account. `AuthorizeActiveRole::handle` holds the user and Party locks through its callback; future role-scoped mutations must supply the record's server-resolved Party and the expected context revision there. Missing/revoked verification, an inactive or revised membership, the wrong role/owner, a stale context or missing Auditor MFA fails closed. A suspended membership's reinstatement does not restore a previously selected role. GET role context uses the same authorization boundary. The identity Resource currently exposes only `identity.select_role` and `identity.view_role`, never financial, company-mandate or audit-filing permissions.

| Operation | Named web route | Named API route | Shared application entry point |
|---|---|---|---|
| Read identity/launcher facts | `dashboard` | `api.v1.identity.show` | `GetIdentityContext` / `IdentityContextResource` (`identity-v2`) |
| Resolve reviewed person identity | `identity.people.resolve` | `api.v1.identity.people.resolve` | `ResolveVerifiedPerson` |
| Provision/change a membership | `identity.memberships.update` | `api.v1.identity.memberships.update` | `ChangeMembership` |
| Select active role | `identity.active-role.store` | `api.v1.identity.active-role.store` | `SelectActiveRole` |
| Read authorized role context | `identity.roles.show` | `api.v1.identity.roles.show` | `AuthorizeActiveRole` |

Web commands use authenticated, CSRF-protected Laravel routes; API commands use Sanctum and require the relevant `identity:manage`, `identity:select-role` or `identity:access` ability for bearer tokens as well as current server-side authority. Both adapters serialize the same shared Resources; the Inertia dashboard consumes the shared result directly. `resources/js/types/identity.ts` defines the type-only client contract and named Wayfinder routes expose the matching transports.

Fortify remains the single authentication entry point. `CreateNewUser` keeps framework validation and adapts the ID returned by `RegisterIdentity` to Fortify's User return type. `ResetUserPassword` remains the existing framework adapter; neither is duplicated for the API. Consent, recovery policy, complete staff permissions, entity authority, Auditor biometric binding and transactional authorization remain unfinished Phase 1 work. The local regressions cover web/API parity, cross-login restrictions, stale/replayed commands, immutable auditing and four PostgreSQL races; they do not replace latest-commit non-author review or checkpoint acceptance.

### Checkpoint 1 entry and navigation adapters — 2026-09-24

PR #97 merged to `dev` at `2774c03` after its five hosted gates passed. Aminu explicitly waived Erastus's review for that PR; this is a scoped exception, not a claim that a review occurred or a waiver for later checkpoints.

`RoleHomeController` publishes `investor.home`, `business.home` and `auditor.home` through `AuthorizeActiveRole`; GET requests never select a membership. These entry pages expose verified identity facts only. Denied browser requests render `identity/access-denied` without stale role props; JSON clients retain stable error codes. Financial dashboard data remains outside checkpoint 1.

`GetStaffAccess` and `ConfigureStaffAccess` use `IdentityAccessStore` and its existing transactional adapter. `staff_accounts` grants only `admin.open`, separately from `identity_operators` and marketplace roles. The trusted deployment command `identity:staff <user-id> --reason="..." --no-interaction` grants this entry capability, with `--revoke` to withdraw it. Provisioning enforces a separate account, verified email and effective confirmed MFA, records immutable evidence and refuses established/shared participant identities. Revocation remains available without those prerequisites. A disabled staff account cannot be resolved into a participant identity. `staff-access-v1` exposes only `contract_version`, `can_open_admin` and scoped `allowed_actions`, with the same Resource on the dashboard, authorized `admin.home` and `/api/v1/staff-access`. This does not grant financial, directory, identity-management or maker-checker operations; the full staff matrix remains open. No real staff grant is created by the migration or a seeder.

`SaveRoleBookmark` and `GetRoleBookmark` authorize and lock the current user and Party through the existing access port. `role_bookmarks` has one position per account and role, bound to the selected membership ID/revision. The shared `BookmarkDestination` allowlist currently accepts only the three implemented role homes, no parameters, and an optional `section=overview|access` query. New screens must add their own validation and record-level authorization before becoming restorable. Revoked or changed memberships cannot restore old positions; obsolete destinations fall back to the authorized role home. Bookmarks contain no financial/evidence props, raw URLs or scroll state. Saves require the current context revision and request UUID, and are audited atomically. Same-command replay returns the current position without overwriting a newer save. The `role-bookmark-v1` Resource serves web and `/api/v1` endpoints; `identity.roles.resume` resolves a fresh authorized redirect for the launcher. API navigation additionally requires `identity:access`; the staff projection requires `staff:access`, which alone grants no authority.

Architecture rules now cover `StaffAccount` and `RoleBookmark` along with the other protected identity records. Local verification for this adapter slice: 857 non-TIA PostgreSQL tests / 4,444 assertions with 100% application line coverage; 337 web tests with 100% lines, statements, functions and branches; zero PHPStan errors; TypeScript and production build pass. PostgreSQL includes concurrent duplicate bookmark saves and saves racing role switches. This adapter slice is committed as `9484b2b` before integration.

The checkpoint 1 integration merges Erastus's `67c4528` snapshot and adapts the launcher to `identity-v2`. Role buttons use the shared JSON selection action with the expected revision and a request UUID; only a matching selected role plus `identity.view_role` navigates to the authorized resume route. Network retries preserve the request UUID, while stale/revoked/MFA/validation failures require refreshed authority. Current selected roles still reauthorize through resume. Staff entry uses only `staff_access`, not marketplace roles or identity-operator status. Focus, visibility and reconnect events reload the applicable access Resources and coalesce duplicate refreshes. The four richer role surfaces remain synthetic previews behind the existing local/testing-only preview route; no financial workflow is enabled.

Combined local verification at `04e191c568bc8e9634d3aed0eb64fb5f858803c7`, published in [PR #98](https://github.com/rozine-rw/rozine/pull/98), passes 1,023 non-TIA PostgreSQL tests / 5,412 assertions with 100% application line coverage, 727 web tests with all four metrics at 100% across 304 authored executable files and exact-SHA D-67 admission, zero PHPStan errors, TypeScript, build, i18n and inventory checks. `vp check` reports zero errors and 41 warnings in the combined branch. `tests/Browser/CheckpointOneBrowserTest.php` runs Playwright CLI against factory accounts in the isolated `rozine_test` database and a temporary application server. It proves real login/CSRF, Investor/Business switching, saved-position restoration and cold reload, denial after another tab changes roles, logout, a separate login's independent selection/position, Auditor MFA gating and staff MFA challenge/entry. Desktop and 390×844 screenshots were inspected under ignored `output/playwright/checkpoint-one/`. Run it explicitly after building assets with `PLAYWRIGHT_CLI=/path/to/playwright-cli php vendor/bin/pest tests/Browser/CheckpointOneBrowserTest.php --no-tia --compact`; it is separate from the default PHP suites and requires the installed CLI/browser. Hosted exact-commit checks and non-author checkpoint review remain separate acceptance evidence.

### Checkpoint 2 identity authority and exact arithmetic — 2026-09-24

`AuthorizeStaffPermission` holds the existing dedicated staff/MFA authority locks through the protected callback. Explicit `StaffPermission` roles are analyst, approver, treasury, compliance and superadmin; none grants wildcard access or an underwriting override. `identity:staff --role=...` provisions these roles through the existing audited operator boundary. Registration and HTTP cannot grant them.

`ResolveVerifiedOrganization` requires `businesses.verify`, records reviewed RDB evidence and resolves one organization Party under an advisory lock and unique registry digest. It accepts no tax-identifier namespace and grants no participant membership or entity mandate. `WithVerifiedParties` holds sorted Party locks and verifies the entity plus every required person; it is a verification boundary, not actor authorization. A Business action must additionally check the active role and current effective mandate. `VerifiedOrganizationIdentity` joins the existing protected identity model set, accessible only to identity adapters, model relationships and factories. Concrete-target assertions prevent an empty rule; the identity negative control probes both Party and organization-identity writes.

`App\Domain\Underwriting` contains pure exact-rational calculation only: `CashFlowEvidence`, `EngineScorecard`, `FlatReturnPricing`, `LoanSchedule` and `LoanCapacity`, with `ExactFinancialValue` and `UnderwritingViolation`. Inputs include the complete observation/repayment calendar; domain code does not read clocks, external evidence or persistence. Whole-RWF half-up rules, original-request cutoff, final DSCR, repeat weak-month limits, exposure room and final 5,000-franc quantization follow engineering-2026-09-23.4. Internal score diagnostics are not a public Resource. Evidence validation and immutable calculation publication are separate modules and remain subject to the first-PR boundary gate above.

Local focused evidence: 66 underwriting tests / 250 assertions, 36 staff/organization feature tests / 210 assertions and 10 PostgreSQL identity concurrency tests / 35 assertions. Concurrency includes duplicate registry requests, simultaneous canonical organization resolution and blocking verification/permission revocation until protected work commits. These are local foundation checks, not full checkpoint, hosted CI or browser acceptance.

### Checkpoint 2 command outcomes and Business mandates — 2026-09-24

`App\Application\Operations\Contracts\OperationJournal` is the only entry to immutable `CommandOperation` records through `App\Infrastructure\Operations\EloquentOperationJournal`. Its authorizer runs within the transaction before execution and every replay; callers supply server-resolved Party/staff keys and lock current record authority. PostgreSQL serializes each Party/command/UUID, binds canonical permitted input and target identity, and records the effect plus final outcome atomically. A rejected operation rolls back its inner effects while recording its denial. Unexpected failures leave no committed outcome. Scoped lookup returning 404 does not prove an in-flight command failed. Outcomes cannot be updated or deleted, including after their eight-day minimum lookup-retention date; no purge policy is added. `OperationResource` emits only the agreed public envelope, preserving the recorded HTTP result without disclosing actor keys or request hashes.

`CanonicalJson` binds to the approved JCS package through `JcsCanonicalJson`. This governed profile permits UTF-8 strings, booleans, null, arrays/objects and exactly representable integer JSON numbers. Money, ratios and large counts are strings. It rejects floats, integers beyond 53-bit precision, malformed UTF-8 and U+2028/U+2029 before invoking the package, so unsupported serialization cannot silently produce a purported canonical digest. This is an intentionally bounded input profile, not a claim that the dependency handles every JSON numeric/string value. Tests cover exact large money strings, UTF-16 key ordering and rejection boundaries.

`ConfigureBusinessAuthority` and `WithBusinessAuthority` are the Business authority entry points through `BusinessAuthorityStore` and `App\Infrastructure\Business\EloquentBusinessAuthorityStore`. Only this adapter may reference `BusinessProfile` and `BusinessMandate`. Every version preserves the reviewed profile, complete declared-person set, required signatories, explicit permissions, effective/expiry timestamps, actor, evidence and reason. PostgreSQL rejects mandate updates/deletes. Staff may revoke a mandate after a party loses verification, but cannot activate unverified authority or change a company code away from the verified RDB identity. Sole traders have one verified owner/signatory; companies use their actual mandate without a two-director assumption.

Business commands first lock their aggregate, then the actor and all affected identity Parties in stable order through `AuthorizeEntityRole`. Current Business role/context, the verified entity and every declared person, effective mandate, exact permission and optional mandate version are rechecked through the protected callback. People without view authority receive scoped 404; visible but ungranted operations receive an explicit denial. These actions do not issue a note, publish an underwriting result or approve legal documents. Architecture tests name concrete targets for the journal and Business models; their negative controls reject bypass writes. Full application, evidence, signing and transport integration remain in progress. The foundation passes 1,196 full non-TIA PostgreSQL PHP tests / 6,101 assertions at 100% application line coverage, zero PHPStan errors and all 28 negative-control scenarios. The web foundation chain and `vp check` pass (41 existing warnings). Exact-commit publication and complete checkpoint acceptance remain separate.

| Class | Exception | Owner | Removal gate |
|---|---|---|---|
| `App\Http\Controllers\PulseController` | Directly queries `PulseSignup` and calls `PulseUnderwriting`; it predates the application-action boundary. | Aminu and Erastus — Engineering | Isolate or remove before Phase 1 begins; Pulse product scope remains deferred to Phase 6. |
| `App\Actions\Fortify\CreateNewUser` | Implements a Fortify framework contract inside the starter-kit authentication seam. | Aminu and Erastus — Engineering/Security | Reassess with the Phase 1 identity module; never duplicate it for the mobile API. |
| `App\Actions\Fortify\ResetUserPassword` | Implements a Fortify framework contract inside the starter-kit authentication seam. | Aminu and Erastus — Engineering/Security | Reassess with the Phase 1 identity module; never duplicate it for the mobile API. |

These are exact, temporary exceptions—not namespace-wide ignores or permission for new code to repeat the pattern.

### Checkpoint 2 versioned consent and review decisions — 2026-09-24

`RecordConsentRelease` and `WithCurrentConsent` use `ConsentCatalog` through `App\Infrastructure\Identity\EloquentConsentCatalog`. `ConsentRelease` is an existing-symbol target of the protected identity rule, with a direct-write negative control. The adapter records externally approved text and evidence; it does not itself grant legal approval. Only explicit compliance/superadmin permission `consent.documents.record` can record a release, under current verified staff/MFA authority. A release preserves complete Terms/Privacy text, owner summaries, mandatory disclosures, versions, computed SHA-256 hashes, provenance, actor and reason. Existing kind/key versions cannot be reused for changed content, summary or synthetic provenance. A withdrawal appends a new release and never falls back to an old one. No actual approved legal document is seeded.

PostgreSQL holds an exclusive catalog advisory lock for publication and a shared lock through protected reads/acceptance, so a concurrent release cannot change what a signer accepts mid-transaction. Missing, withdrawn or environment-ineligible documents are unavailable. Synthetic releases are accepted only where `EnvironmentIsolation::canSeed()` allows fixtures; they are unavailable in UAT/production. `ConsentDocuments` matches every exact kind/key/version/hash without numeric-string coercion; application signatures will retain the selected release and receipt. Publication/revocation races pass with the combined 17-test / 58-assertion PostgreSQL suite. The complete PHP suite passes 1,250 tests / 6,262 assertions at 100% application line coverage, with zero PHPStan errors; all three identity boundary probes fail as required and the clean architecture suite passes afterwards.

Erastus reviewed PR #101 at `1740ea9f` with no blockers. The journal's eight days remain a **minimum lookup interval**, not a TTL or deletion permission (MC-01). There is no prune command. Future CFG-05 purge/anonymisation must respect the relevant retention/hold rule and preserve effect uniqueness plus minimal replay tombstones; expiry must never make a committed command executable again. The HTTP `OperationResource` now adds immutable `recorded_at` from the stored outcome, emits fresh response `server_time`, and mirrors `field_errors` as Laravel's `errors` bag. The immutable outcome itself does not change on replay. Business mandates currently represent an explicit **all-of** verified signer set. Arbitrary quorum mandates are not inferred from counts; unsupported/conflicting authority remains unavailable until accurately represented. `MANDATE_STALE` requires refreshed authority and a fresh acceptance attempt.

Empty `field_errors` and `errors` are JSON objects on the wire. When a recorded outcome has no public data, `data` is `null`; populated data remains a JSON object. PHP associative decoding alone cannot distinguish an empty JSON object from an empty list, so transport regressions inspect the encoded response. A Business profile's `industry` is the reviewed display string, not a closed taxonomy or an i18n key. Resources preserve it; the isolated profile fixture uses `Retail`. The Auditor Jobs `sector` is a separate nullable, normalized classification.

### Checkpoint 2 application drafts — 2026-09-24

`CreateBusinessApplication`, `SaveBusinessApplication`, `GetBusinessApplication` and `FindBusinessOperation` use `BusinessApplicationStore` through `App\Infrastructure\Business\EloquentBusinessApplicationStore`. Only the Business adapter, model relationships and factories can reference `BusinessApplication` or `BusinessApplicationVersion`. They join the existing concrete-target architecture assertion and direct-write negative controls. Each committed draft version records the exact whole-franc request, resume pointer, actor account/Party, mandate version and policy. PostgreSQL protects the historical snapshots against updates/deletes. Drafts may be incomplete; no calculation, approval, review-step transition or signature is implied by saving one.

The adapter locks current Business authority before the application, then resolves the actor on the server and serializes the journal request. A repeated UUID cannot create another application or version, a changed body conflicts, and a stale version cannot overwrite a newer edit. A second login for the same Party sees the same receipt only after current authority is checked. Operation lookup verifies target scope and the locked Party identity; it refuses a receipt if account identity changes between initial lookup and authorization. Reads preserve the saved version. Draft save cannot bypass the separately required quote/signature workflow or edit a submitted application. Local proof: 1,289 full non-TIA PHP tests / 6,423 assertions at 100% application line coverage; zero PHPStan errors; the combined PostgreSQL concurrency suite passes 19 tests / 65 assertions. Live page/API contracts and financial publication remain in progress.

### Checkpoint 2 immutable statement sources — 2026-09-24

`IngestStatement`, `GetStatementEvidence`, `ReadStatementOriginal`, `RecordStatementTranscription`, `GetStatementTranscription` and `FindStatementOperation` under `App\Application\Evidence` use the `StatementStore` port. Its `App\Infrastructure\Evidence` adapters are the only context allowed to reference `StatementEvidence`, `StatementOriginal`, `StatementExtraction` and `StatementTranscription`, apart from model relationships and factories. Architecture rules assert these concrete targets exist; the `evidence-boundary` negative control plants bypass writes against each model and requires failure followed by clean teardown. The new protected seam requires non-author review with this implementation PR.

Ingestion holds current Business authority before the evidence aggregate and journal. It requires `application.save`, resolves the Party on the server, compares the expected evidence revision, and journals the original-byte digest rather than source contents. Repeated operations commit once; duplicate bytes within the same Business do not create another original or advance evidence. New source bytes append a revision. Original bytes, filenames and extracted text use Laravel encrypted casts in private database columns. The receipt, source and initial `pending` extraction commit atomically; no parser executes inside ingestion or its Business/journal locks. PostgreSQL rejects original/extraction updates and deletes. Original downloads and detailed transcription reads require current `application.save` authority and verify integrity; `business.view` permits scoped metadata only. Manifests use generated display filenames and do not load or expose original filenames, source bytes or extracted text. There is no public storage URL or automatic purge.

`StatementSource` bounds originals to 10 MiB, rejects control/bidirectional filename characters, validates PDF/UTF-8 CSV bytes and computes SHA-256 on the exact input. `StatementTextExtractor` binds to `IsolatedStatementTextExtractor`. The scheduled `statements:extract` command uses the `StatementExtractionQueue` port to scan durable pending records; it refuses to run inside a caller transaction. Each parser child has 128 MiB memory, five CPU seconds and eight wall seconds, receives bytes over stdin without application environment values, and disables the PDF parser's plaintext `tmpfile` fallback. Parser termination, timeouts, errors and corrupted originals append a stable `needs_review` result. The final short transaction serializes on the original and appends once even if workers race. There is no enqueue gap: a worker interrupted before finalization leaves a durable pending record for the next scan. UUID replay keeps the original receipt; manifest GET returns the latest extraction status.

The approved parser retains no image content and keeps its per-stream decode bound in addition to process limits. UTF-8 CSV extraction preserves original text, recognizes BOM/CRLF/CR exports and quoted multiline records, and rejects inconsistent structures. Extracted text is bounded to 2 MiB. Successful ingestion is always `INGESTED_NOT_AUDIT_APPROVED`; extraction never establishes source authenticity, complete coverage, financial classification, verified snapshots or credit approval. The worker is a mechanical private process, not an authorization bypass for a caller.

`statement-classification-2` requires every own-account transfer to name a reciprocal equal/opposite leg on another declared rail. Cross-month settlement is permitted only when both legs are in the supplied window. Missing legs, a counterpart classified as revenue, and negative financing are rejected. Monthly observations separately disclose financing inflow and transfer inflow/outflow. A rail first activated mid-window starts at zero; a declared closure ends at zero. Existing first-window balances and overdraft continuity remain factual inputs. These checks do not replace independent source verification.

`RecordStatementTranscription` reconciles factual transcriptions against original IDs from the authorized Business catalog. Its immutable encrypted payload binds the declared rails/months/transactions, exact source hashes, classification version, evidence revision and unverified observations under a JCS/SHA-256 digest. A correction links a new record to its predecessor, with actor and recording time; PostgreSQL refuses update/delete. The evidence lock and journal serialize retries and competing revisions. Reads verify the canonical digest and Business provenance, and expose whether the record still matches the current evidence revision. New source bytes make prior transcriptions stale. Receipts contain identifiers/digests rather than financial source contents. Source authenticity, complete rail declarations and Auditor verification remain separate requirements; a reconciled transcription never becomes a verified observation merely by being stored.

Local evidence: 1,426 complete non-TIA PHP tests / 6,894 assertions at 100% application line coverage, zero PHPStan errors, and all four evidence-boundary probes caught with clean teardown. Tests cover binary PDF preservation, encrypted storage, plaintext exclusion from manifests/receipts, stale revisions, exact retry, pending-lineage persistence rollback, current-authority denial, malformed inputs, original integrity, immutable rows and migration rollback. The 24-test / 86-assertion PostgreSQL suite includes same-Party imports/reconciliations and competing evidence revisions by different mandate holders. Transcription tests also prove encrypted history, source-scope checks, append-only corrections, digest validation, current-authority replay and complete rollback after a persistence failure. Review corrections add hostile-PDF containment, immutable pending/terminal outcomes, read-permission separation, encrypted filename migration, matched transfer legs, calendar boundaries, maximum-size benign input and concurrent worker finalization. Full correction-gate counts are recorded in the Phase 1 checklist. The maximum-size original test bounds parent allocation growth below 160 MiB; local PHP-FPM was inspected at 512 MiB, while live HTTP/FPM acceptance remains open. HTTP upload/download and Auditor verification remain in progress; this is not complete checkpoint 2 acceptance.

### Checkpoint 2 pure Auditor operating rules — 2026-09-24

`App\Domain\Auditor` contains `AuditorStanding`, `AuditorDispatch` and `AuditAssignmentClock` under the existing pure-domain architecture rule. Current standing requires an active certificate valid through its printed expiry day in Rwanda and an evidenced, non-future ICPAR check no older than 30 days. Dispatch additionally checks current location verification (365 days and after any move), maximum three active engagements, rotation after three consecutive reports, financial/role/family/business and unresolved declared conflicts, and the full 24-calendar-month role-tie lookback. Candidate selection uses fewest active assignments, then oldest assignment, then stable partner ID; no eligible candidate leaves an operational queue.

Distance admission expects a server-resolved conservative whole-metre geodesic upper bound and includes both uncertainty radii in the 30 km limit. The pure evaluator does not establish those facts or implement their authoritative geodesic source. Flash acceptance is one hour with completion fixed at 24 elapsed hours from original dispatch; reassignment cannot extend that completion boundary. Routine acceptance is four hours and the visit deadline is 48 elapsed hours after acceptance. Three automated attempts exhaust into Operations. Identity/MFA, persisted source authority, atomic assignment transitions, report verification and seals remain application/infrastructure work.

Local proof: 59 focused policy tests / 134 assertions; full non-TIA PHP suite 1,556 tests / 7,338 assertions at 100% application line coverage; zero PHPStan errors. Tests include exact freshness/radius/deadline boundaries, invalid/future facts, calendar lookback at leap day, deterministic ties, exhausted attempts and preserved Flash deadlines. All five hosted checks pass at `d138a2349e328eb5b3832d675bbfb34ec41d427d`; [Erastus approved this policy slice](https://github.com/rozine-rw/rozine/pull/101#pullrequestreview-5302548428). Routine monthly report/co-sign deadlines come from the reporting calendar, independently of the assignment visit clock.


### Checkpoint 2 persisted Auditor accreditation — 2026-09-24

`SubmitAuditorAccreditation`, `WithdrawAuditorAccreditation`, `SetAuditorAvailability`, `RecordAuditorStanding`, `GetAuditorProfile`, `FindAuditorOperation` and `ReadAuditorCertificate` use `AuditorProfileStore`. Only `App\Infrastructure\Auditor`, model relationships and factories may reference `AuditorProfile`, `AuditorProfileVersion` and `AuditorCertificate`. Concrete-target assertions and one direct-write negative control per model protect the seam. [Erastus approved the new persistence boundary at `9093c8ad`](https://github.com/rozine-rw/rozine/pull/101#pullrequestreview-5302887071).

The adapter now acquires current actor/identity/MFA locks before the advisory Party aggregate and current profile row, then the command journal. This order was aligned with persisted assignment dispatch after the initial accreditation review. It resolves the participant Party server-side and rechecks the active Auditor context, including on replay and lookup. Initial submission and renewal retain distinct journal names. Following Erastus’s live-adapter review, the domain also requires first submission when no approved certificate exists and renewal when one does, matching `AccreditationView`; a wrong command records `ACCREDITATION_ACTION_NOT_ALLOWED`. The check runs inside the journal operation so old receipts remain historical results after later approval. Expected revisions serialize all profile changes, including availability and staff decisions. Submission, private encrypted original bytes/filename, encrypted profile state, immutable encrypted history and the minimal receipt commit together; validation or persistence failure cannot leave partial accreditation. Raw files and filenames are excluded from journal outcomes and generic model serialization. Certificate reads require the current owner Auditor or `audit.partners.verify` staff, and verify the exact byte digest and size.

A submitted certificate is an unverified claim. It cannot grant standing or replace an approved licence. Approval names the current pending submission; withdrawal or a competing change makes a stale approval fail. Rejection preserves existing approved standing. An explicit staff check records its evidence reference, time and reason; approval/recheck also require a currently verified person. Suspension and revocation disable acceptance immediately, and a routine recheck cannot reactivate them. Availability records the Auditor's intent; every dispatch/co-sign action must still run the current-standing gate, so a stale check or expiry cannot be treated as current authority. Native attestation, location authority, assignment persistence, automatic expiry presentation, live Resources/routes and browser acceptance remain separate work.

Certificates are bounded to 10 MiB PDF/PNG/JPEG uploads and are identified without decoding untrusted documents/images in the command transaction. Content remains private and unverified until independent staff review. PostgreSQL rejects updates/deletes to certificates and profile versions; there is no purge or public object URL. All storage and race tests use the isolated test database. Local proof: 1,616 full non-TIA PHP tests / 7,601 assertions at 100% application lines; zero PHPStan errors; 34 PostgreSQL concurrency/worker tests / 134 assertions; all three Auditor-model bypass probes caught with clean teardown and a matching generated inventory. The persistence review is recorded above; hosted checks and whole-checkpoint acceptance remain separate gates.


`GetAuditorAccreditation` reads the profile and pending certificate hash under the same aggregate/identity locks, then `AccreditationView` derives fresh standing and Rwanda-calendar expiry facts. `AuditorAccreditationResource` is the shared serialization boundary for that authorized projection. It excludes staff check references and original filenames; an explicit rejection explanation remains visible so the Auditor can correct it. Pending uploads carry their real hash with capture position/time and device attestation unavailable. No gallery upload becomes attested field evidence. A saved availability preference can remain on while current standing is false; callers and the UI must use both facts. Pausing stays possible while standing has lapsed. Approval of a now-expired pending submission returns `ACCREDITATION_SUBMISSION_EXPIRED` as a journaled 422 field error, without altering dispatch/co-signature denial rules. Full local proof is 1,625 tests / 7,656 assertions at 100% application lines and zero PHPStan errors; live routing and command response decoration remain open.

### Checkpoint 2 reviewed audit locations — 2026-09-24

`VerifyAuditLocation`, `MarkAuditLocationMoved` and `GetAuditLocation` use `AuditLocationStore`. `AuditLocation` and `AuditLocationVersion` join the protected Auditor adapter boundary. Each location refers to exactly one Auditor office Party or Business profile through a unique foreign key; PostgreSQL rejects empty/ambiguous subjects, invalid revisions and history updates/deletes. State, precise coordinates, evidence references and historical reasons are encrypted and excluded from generic serialization. The staff commands commit location, immutable version and minimal journal receipt together.

Office verification requires current `audit.partners.verify` staff authority and a verified person. Premises verification goes through `WithBusinessReview`, which locks the Business and checks current `businesses.verify` staff authority, its active mandate, every declared person and the actual registered entity. It does not grant an Auditor the Business role. A staff read or move invalidation can still proceed after underlying verification/mandate lapses, but cannot grant a new verified location. Office transactions now lock staff/required identity before the location; premises transactions hold the Business and authority locks before their location. The persisted assignment slice aligns this ordering across both adapters.

Verification records validated WGS84 coordinate text, uncertainty in whole metres, an evidenced UTC verification time within 365 days, and a private reason. A move clears all current coordinates, uncertainty and verification, even in the same second as the old check; history remains intact. A subsequent verification must be strictly later than that move, including in dispatch eligibility; equal whole-second timestamps are ambiguous and fail closed. This closes Erastus's review note at `0e360afe`. Exact retries return the original receipt without reviving old state, and concurrent revisions cannot overwrite one another silently. This is staff-evidenced registered-location authority; it does not assert device attestation or native field-capture assurance.

`Wgs84Distance` implements the Vincenty inverse on WGS84, adds a 1 cm numerical allowance and rounds upward to whole metres. Tests compare independent GeographicLib reference vectors, meridian/equator/antimeridian cases, coincident points and both uncertainty radii at the admission boundary. Nonconvergence returns unknown distance, which dispatch rejects; it never substitutes a spherical estimate to admit a candidate. Assignment persistence must calculate from these stored records and apply freshness, standing, capacity, rotation and conflicts in the same transaction. Live participant move reporting, assignment binding and browser acceptance remain open. Local validation is recorded in the Phase 1 checklist.

### Checkpoint 2 reviewed Auditor independence — 2026-09-24

`RecordAuditorIndependence` and `GetAuditorIndependence` use `AuditorIndependenceStore`. The protected Auditor adapter owns `AuditorIndependenceReview` and `AuditorIndependenceVersion`; other modules cannot directly read or write them. Each Business/person pair has one current revision and encrypted immutable history with actor, evidence reference, reviewed time, reason and policy. Only current Operations staff with `audit.assignments.manage` and the Business verification permission may record/read it. Recording additionally verifies the current Business mandate, registered entity, every declared person and the reviewed person; a review does not create Auditor membership or accreditation.

`WithBusinessReview` accepts additional person IDs for this verification. The identity boundary locks their union with all mandate people in one sorted set, avoiding separate Business-person then Auditor-person lock acquisition. The Business row serializes first-review creation and revisions, followed by current staff/Party authority, the review row and command journal. Current permission and authority are checked before retry replay. A review/history/receipt persistence failure rolls all of them back, and PostgreSQL rejects history updates/deletes.

The review records explicit financial-interest, current/recent role, family/business and unresolved-conflict facts. Strict booleans and UTC times prevent coercion; empty/control-character evidence is refused, and an older observation cannot replace a newer check. Submitted false flags cannot clear a current Business mandate relationship. Missing review, a changed mandate or an invalid/future review timestamp requires review. No new conflict-review cadence is selected. These are documented facts, not a universal clearance: dispatch must additionally resolve actual financial interests, mandate/role history and unresolved declarations, and combine those restrictions without allowing this review to override them. Assignment persistence and live transport remain open; local validation is recorded in the Phase 1 checklist.

### Checkpoint 2 persisted audit assignments and conflicts — 2026-09-24

`AuditAssignmentStore` implements `RequestAuditAssignment`, `RespondToAuditAssignment`, `GetAuditAssignment`, `FindAuditAssignmentOperation`, `AdvanceAuditAssignment` and the trusted `AdvanceExpiredAuditOffers` worker. `AuditAssignment`, `AuditAssignmentVersion` and `AuditConflictDeclaration` join the protected Auditor-model boundary. One offered/accepted/Operations engagement per Business is enforced in PostgreSQL; history and conflict declarations cannot be updated or deleted. State, selection inputs, mandate/Business revisions, private reasons and conflict kinds are encrypted. This is actual persisted offer/response work; the completion transition still belongs to the pending sealed-report boundary.

The Business boundary owns mandate facts and retained role relationships. Its audit context distinguishes staff assignment authority, a current Auditor actor and the internal expiry worker; an Auditor is not granted Business membership to read an assigned file. The Identity adapter verifies required Business identities while filtering ineligible candidate Parties, so one invalid candidate cannot block the remaining pool. Lock order is Business, actor User when present, the union of all Parties sorted by ID, sorted Auditor profiles, premises/office locations, and independence records. Responses lock the assignment before the command journal; initial requests lock their journal before looking up the assignment. Both paths first hold the Business lock, so they serialize on that Business. Profile and office review adapters use the same actor/Party-before-aggregate ordering. Candidate identities are discovered before locking; newly accredited candidates join the next dispatch. The queries batch memberships, active loads, previous offers and conflicts.

Dispatch uses current stored standing, registered WGS84 locations with uncertainty, active engagements, the last actual offer, reviewed external conflict facts, immutable mandate history and unresolved declarations. A false review cannot clear a current/recent mandate tie or a later declaration. Unknown or stale reviewed evidence routes to Operations. These manual external-conflict facts are the available Alpha source, not proof that a future platform financial-interest source can be skipped; Holding/Order integration must add that authoritative restriction before its surface is enabled. Completed-engagement rotation is read from stored completed jobs; no action in this slice can mark a report completed or verified.

Acceptance rechecks current authority, capacity, eligibility, revision and the server deadline. Different logins linked to the same Party share the capacity limit and operation scope. Decline/conflict atomically retains its reason, removes the old recipient's private read access and selects an eligible untried alternative; no alternative or exhausted attempts goes to Operations. New and replayed receipts expose no replacement partner identity. Current role authority still permits the old recipient to retrieve their own minimal receipt, while old retries cannot regain file access. A new current review that finds a conflict also denies private reads.

`audits:advance-offers` runs every minute with overlap prevention, bounded batches and a fresh locked eligibility check. Its scan compares explicit UTC instants rather than timezone-less bound dates. Concurrent workers recheck the locked state; only one can advance an expired offer. System transitions record a null user and their own command, not a forged staff actor or a participant request UUID. Routine acceptance remains four hours with a 48-hour visit deadline from acceptance; Flash acceptance remains one hour and completion 24 hours from the original dispatch. Three automated offers exhaust to Operations without resetting the original deadline. Resolving that queue, report/evidence completion and shared live HTTP/browser acceptance remain open.

Local evidence is recorded in the Phase 1 checklist. No application database migration, deployment, native-device acceptance or whole-checkpoint completion is claimed by these isolated test results.

## Consequences

- Laravel owns both web and mobile business logic without creating a second backend.
- Inertia remains a server-driven web transport; native clients consume `/api/v1` later.
- Shared Resources make schema parity testable while shared actions preserve behavioral parity.
- Financial/evidence transactions stay inside one database boundary and can use PostgreSQL locking and idempotency controls.
- The architecture suite grows alongside modules and cannot replace behavior, authorization, concurrency, or Resource contract tests.

## Approved Phase 0 contract

- [x] Use the documented Domain/Application/Infrastructure/HTTP namespace boundaries.
- [x] Start post-MVP native API versioning at `/api/v1`.
- [x] Use Eloquent API Resources as the shared serialization boundary, not the business-logic boundary.
- [x] Preserve one shared application/domain layer for Inertia web and mobile API transports.
- [x] Accept the three exact legacy dispositions. `PulseController` must be isolated or removed before Phase 1 starts; the two Fortify actions remain controlled framework exceptions and must be reassessed with the Phase 1 identity module.

Acceptance closes the architecture decision. It does not close the remaining implementation evidence: `PulseController` isolation/removal, the expanded module rule catalog, controlled architecture-test failures, and exact-SHA hosted results must still pass before Phase 1 governed behavior begins.

### Accepted-assignment statement access — 2026-09-24

`WithAcceptedAuditAssignment` holds the current Business, verified mandate/Parties, actor/MFA, standing, independence and accepted-assignment authority through a protected callback. The callback receives only the resolved assignment/Business/Party IDs, revisions, kind and mandate version. It does not grant offered or former recipients access, and a stored acceptance does not bypass a later standing or authority withdrawal. Saved availability and the capacity limit govern new offers; pausing or reaching capacity does not hide already accepted work.

`GetAuditStatements`, `ReadAuditStatement` and `GetAuditTranscription` use this boundary inside the Evidence adapter. The Auditor module never reaches into protected statement models. Business authority remains the owner of ingestion/corrections. Both audiences share integrity-checked original and canonical-transcription reads; Auditor downloads use neutral names. A single current-authority read returns the source manifest with its current revision and an explicitly unverified transcription, preserving amendment links and stale-history reads. Neither extraction nor read access certifies complete rails, source authenticity, factual review or an approved monthly snapshot.

Proof: 82 focused feature tests / 394 assertions, 1,764 full non-TIA PHP tests / 8,371 assertions at 100% application lines, zero PHPStan errors and a matching generated inventory. The 47-test / 178-assertion PostgreSQL lane proves that Business evidence writes, conflict reassignment, membership revocation and standing suspension wait for an in-progress protected read and are observed by the next read. HTTP/Resource binding, procedure findings, verified monthly snapshots and seals remain separate checkpoint work.

### Immutable factual source verification — 2026-09-24

`RecordStatementVerification`, `GetStatementVerification`, `GetAuditStatementVerification` and `FindStatementVerificationOperation` extend the existing `StatementStore` port. Only the Evidence adapter, model relationships and factories can reference `StatementVerification`; the architecture assertion and direct-write negative control cover this additional model. PostgreSQL rejects update/delete, while encrypted payloads and minimal hash/identifier receipts keep source contents private. Corrections append a new linked revision rather than changing a previous attestation.

Recording holds current accepted-assignment authority through the evidence lock and journal. It compares assignment, evidence and verification revisions, checks every exact original, verifies the current transcription digest, recomputes reconciliation, and requires the complete source-review subset of `MVP-AUP-1`. The qualified Auditor explicitly attests the inventory, authenticity, reconciliation/classification, debt schedules, baseline/gaps and draw/exception checks with references. Amounts and schedules are factual inputs, not client-supplied credit decisions. The snapshot binds source hashes, classification/procedure/policy, current mandate and accreditation facts, exact monthly observations, actor and recording time. A 36-month persisted-source test feeds those observations into `CashFlowEvidence` without substituting controller-supplied financial facts.

Reads distinguish historical attestation from current eligibility. New evidence/reconciliation, a correction, changed Business authority, redispatch or a newly discovered conflict makes the previous verification non-current. Suspension, revocation, missing approved accreditation, lost verified identity or inactive/conflicting Auditor membership also withdraw current eligibility. Historical source verification does not expire merely because the Auditor later pauses dispatch, their licence later expires, or they withdraw a pending renewal while approved facts remain. New attestations and final co-signing still require current standing. Receipt replay and lookup require current assignment authority and reject an identity relink to a different Party. Replaying a successful receipt after new evidence never restores currentness. PostgreSQL tests cover duplicate UUIDs, competing revisions and verification racing ingestion, transcription, conflict, independence, standing and membership changes.

The snapshot's top-level classification version binds to its immutable transcription and each observation. A classifier upgrade leaves historical facts readable but non-current. Reads compare the actual transcription digest and canonical payload; source or snapshot corruption is an unjournaled server error. Revision conflicts return the specific assignment, evidence or verification revision that failed, in that order. The Identity port's Party-active predicate supplies current facts only and never grants access. Before any future authoritative calculation consumes a snapshot, its source Auditor authority must remain protected through the effect under the global lock order. Business and the currently authorized replacement Auditor may read prior factual attestation and professional attribution; reassigned or conflicted former Auditors lose private evidence access.

Nested review input is validated before field access: exact key sets, arrays/lists, text, hashes, booleans, canonical money and month keys all receive stable domain denials. Only the constructed validated review enters the immutable payload; malformed input produces a recorded 422 without a partial verification. Duplicate debt source identifiers are rejected.

The protected assignment facts now bind the canonical hash of the normalized stored mandate, standing status, and independence review ID/revision/check instant/evidence reference/state hash. Each original records its exact hash and extraction ID/revision/parser/status/reasons/count as observed during the source review. Qualified manual review may precede extraction or resolve an extraction that needs review; the snapshot retains those states explicitly. Later parser output appends lineage and does not rewrite the earlier manual attestation. A later accepted Auditor can append a correction with their own authority while the former Auditor's immutable history remains intact and no longer grants private access. Synthetic verification factories provide the complete historical shape with an unsupported policy and never confer current authority.

This is a factual source verification, explicitly `report_approval: not_cosigned`. It does not complete premises/native-capture evidence, final procedure findings, a co-signed published report, cryptographic seal or application signature. Those are separate checkpoint requirements. Full validation evidence is maintained in the Phase 1 checklist.


### Assignment review corrections — 2026-09-24

Participant reads, receipt lookup and acceptance lock only the acting Auditor's candidate rows, alongside the required Business/actor/mandate authority. Dispatch, decline, conflict and expiry still take the full sorted candidate set. The Auditor Party lock is shared by these transitions and holds through the active-count check and accepted-state write, preserving the three-engagement ceiling across different Businesses and logins. Active capacity counts accepted engagements; pending offers neither reserve a slot nor authorize private source access. An unrelated Auditor can update availability while another's private evidence read is in progress.

PostgreSQL connections use UTC. Dispatch recency reads the exact UTC `offered_at` from the latest immutable offer snapshot (ordered by its server-generated ULID), avoiding reinterpretation of an offsetless model timestamp. Tests explicitly switch the session ahead of and behind UTC and still accept a current offer. Entering Operations clears the previous `offered_at`; the immutable history retains it.

A former recipient whose own offer ended in expiry or staff advance gets `ASSIGNMENT_ACCEPTANCE_EXPIRED` (409), without the replacement identity or revision. This is derived from the first immutable transition after their last offer, so a voluntary decline or conflict cannot be misreported as expiry just because a subsequent recipient times out. Late decline at `accept_by` is refused; a current safety conflict remains recordable. A preliminary participant check denies unrelated IDs before Business-authority details, and locked authority/participation are still rechecked before any read or effect.

The expiry worker rolls back and logs a failed row, then continues its bounded batch. Logs contain only the opaque assignment ID and exception class, never source contents or exception messages. Corrupt records remain for Operations investigation and retry. Accepted evidence access remains available for authorized late remediation after the SLA deadline: it neither extends the Flash clock nor completes/co-signs a report. Native package/capture cutoffs and final report checks remain separate requirements. Validation counts and remaining transport-contract work are tracked in the Phase 1 checklist.


Decline commands carry `reason_code` (`unavailable`, `capacity`, `location`, `other`). The domain requires a factual explanation for `other` and validates optional supplied explanations for the remaining codes. The code is included in the command hash and immutable receipt; the private explanation remains in encrypted assignment history. Missing/unknown codes and invalid explanations are recorded 422 field errors and leave the offer unchanged.

### Own-conflict reads — checkpoint 2 S-C

`GetOwnAuditConflict` and `ListOwnAuditConflicts` extend the Auditor assignment port. Reads hold the actor and Party through current Auditor-role/MFA authorization and projection. They do not require continuing Business mandate or professional standing: a qualified-role holder can read their own historical declaration after assignment access is withdrawn or standing is suspended. Revoked membership, missing MFA, changed identity and stale role context still deny access.

The response contains the declarant's immutable conflict ID, kind, recording instant and private note, plus only `reassignment_pending`, `reassigned` or `closed` for the current resolution. It carries no replacement identity, source documents, selection basis or new assignment revision. The register port is bounded to 1–100 records (the HTTP surface caps pages at 50), uses a descending opaque-ID cursor, and retrieves its minimal assignment status in one query. The query takes no Business or assignment locks after the identity lock, preserving the global lock order. PostgreSQL probes prove membership withdrawal waits through both receipt and register projection.

New `CONFLICT_RECORDED` journal receipts bind the conflict ID and coarse resolution at recording time. Private text remains encrypted in the conflict declaration and is never copied into the journal. This historical command outcome is separate from a later current-status read. `ProjectAuditAssignmentOperation` hydrates only the permitted own declaration and preserves the recorded resolution, even when Operations has since closed the case. Current receipt/register reads show the later resolution. The shared operation Resource preserves the recorded clock, refreshes response time, and carries both field-error names for recorded 422 recovery.

### Scoped Jobs and Operations transports — checkpoint 2 S-C

`ListAuditJobs` discovers at most 50 offered/accepted assignment IDs under current Auditor identity, then reauthorizes each minimal case through `WithCurrentAuditAssignment` and the Business application port. Business authority, current standing, independence and assignment ownership remain locked throughout each application read. A final identity check rejects actor/context changes; lost per-case authority omits that row, while unexpected failures propagate. Discovery does not acquire Business locks while holding the actor lock. PostgreSQL tests cover simultaneous conflict, standing, membership and draft changes during the actual protected application read.

The Resource splits each bounded page into `eligible` and `assigned`, with a whole-page continuation link even if authorization changes leave a scanned page empty. The public maps are `routine` → `monthly`, `accepted` → `assigned`, and an accepted `deadline.due_at` from the original Flash completion or Routine visit clock. Per-record actions are authoritative. Missing drafts, evaluation values, relative map positions and procedure progress remain unavailable; the monthly report section is null until S-D. Offered summaries never grant original-evidence access, which still requires an accepted assignment. These draft summaries are not pinned underwriting or report inputs; S-A/S-B/S-D must bind immutable versions at their protected effect.

Web and `/api/v1` use the same Jobs, file and private-conflict Resources and command actions. The dedicated assignment-operation lookup preserves same-UUID outcomes under current authorization. Web pages use Inertia props, API read Resources use Laravel's `data` envelope, and commands use the shared OperationResource. Private GET responses are not cached. API reads and commands separately require `auditor:read` and `auditor:command`; token abilities never replace current Party/MFA/record authorization. Erastus owns the matching U-B components and browser tests; their integration and acceptance remain separate from these server transports.

Operations gets a minimal case read plus revisioned, journalled redispatch and close commands. Every path holds current `audit.assignments.manage` staff permission; API abilities are additionally `staff:audit:read` and `staff:audit:manage`. Redispatch reruns current Business authority and candidate selection, preserves the original dispatch/deadline/tried set, and cannot exceed three offers or the original Flash deadline. No eligible candidate leaves the case in Operations with an explicit pending outcome. Closing is terminal for that assignment and records a separate `closed` state, encrypted history/reason and closure instant. It never creates a completed audit/report or rotation credit. The database requires a null recipient for closed cases and still reserves `completed_at` for actual completion. Concurrent duplicate and competing resolutions produce one winning revision; staff revocation waits through a protected Operations read.

The [S-C review at `6b001418`](https://github.com/rozine-rw/rozine/pull/101#pullrequestreview-5311847230) identified four corrections. A closed engagement cannot be restarted by the unqualified staff request: it returns a recorded `AUDIT_ENGAGEMENT_CLOSED` refusal with the original assignment ID and revision. Its original deadline, attempts and tried set remain unchanged, including when closure races a request. A later legitimate application/report trigger needs an explicit engagement identity; a staff retry is not such a trigger. Staff calls perform a read-only permission preflight before any case or Business lookup and retain the existing permission check inside the Business-first protected transaction. The preflight acquires no row lock and cannot authorize a later effect after revocation.

API Jobs, file, conflict, operation and profile navigation uses `/api/v1`; the API launcher link returns identity context, and the unavailable API home is null. Web navigation remains on web routes. Command `data.next` follows the calling transport, including same-UUID recovery. Staff resolution requires both `staff:audit:read` and `staff:audit:manage` because it returns a case projection; case/operation reads need the read ability. Scoped staff permission is independently required for all of them. Withdrawn staff web case/operation reads render the denied Inertia page without stale case props.

The application summary prefers the most recent submitted application over a newer draft. If none has been submitted, the current draft remains available for pre-submission evidence preparation. This corrects draft replacement of submitted facts; it does not substitute for the assignment/application/revision binding required by S-A/S-D. The conflict receipt deliberately reports the current coarse resolution: a replacement's completed audit is `reassigned`, and a replacement offer that expires without another candidate becomes `reassignment_pending`. The immutable command outcome keeps its originally recorded resolution. `recorded` is not currently emitted by this server projection.

Recorded 422 receipts carry `code`, `field_errors` and the Laravel `errors` alias. Before journal admission, domain rejections carry `code`, `message` and `errors` for a 422; Form Request validation uses Laravel's `message` and `errors`. These structural failures have no recorded operation to recover. Remaining review follow-ups are the read-lock/background-list optimization, explicit application engagement binding, closed-row database immutability/rollback policy, consistent port/HTTP page limits, shared dropped-record codes and offer-time ordering with a stable cursor. Their checklist items remain open.

### Versioned Alpha credit facts — checkpoint 2 S-A/S-B

`RecordIsolatedBusinessCreditFacts` is the trusted fixture publisher through `BusinessCreditFactsStore` and `App\Infrastructure\Business\EloquentBusinessCreditFactsStore`. It is not an HTTP input or a production underwriting override. Current `businesses.verify` staff/MFA authority and `EnvironmentIsolation::assertSeedingAllowed` are mandatory before publication or UUID replay. The Business aggregate is locked before protected staff authority; the journal and append-only source revision commit in the same transaction. A staff member cannot infer an unknown Business through a failed permission check.

`BorrowerCreditFacts` accepts explicitly sourced platform history, obligations and restrictions, with exact money strings, complete identified schedules and validated conduct. Unknown history is null; absence of a source never means first-time borrowing, zero exposure or no restriction. These initial records are explicitly `isolated_alpha` with synthetic provenance. A withdrawal appends a revision, and reads never fall back to an earlier available revision. Facts, provenance and reason are encrypted; the immutable journal receipt contains only source identity/version/hash/status. PostgreSQL prevents update/delete of every source revision.

The internal `withCurrent` port holds the Business lock through the caller's effect. The calling Business action must first authorize its actor; raw credit facts are never an audience Resource. It checks the canonical digest and returns unavailable when fixture access is disabled. `BusinessCreditSnapshot` joins the concrete Business-model architecture boundary, and the negative-control loop proves that an Application-layer bypass write fails and that the restored suite passes. Focused evidence: 55 tests / 184 assertions, four PostgreSQL concurrency tests / 14 assertions and zero PHPStan errors.

Checkpoint 2 submission does not issue a Note or reserve funds. The actual loan/servicing history adapter remains checkpoint 3/4 work, and the financial commitment must revalidate authoritative exposure there. The Alpha fixture adapter does not claim that a live ledger or production history integration exists.

### Immutable application quotes — checkpoint 2 S-A/S-B

`EvaluateBusinessApplication`, `GetBusinessApplicationQuote`, `SaveBusinessApplication` and `FindBusinessOperation` enter the quote workflow through `BusinessApplicationStore` and `App\Infrastructure\Business\EloquentBusinessApplicationStore`. `BusinessApplicationQuote` is a concrete protected Business-model target, accessible only in the Business persistence namespace, models and factories. Its bypass probe belongs to the same first-implementation-PR negative-control gate. The adapter's private `projectQuote` method explicitly selects public offer/refusal fields; the private replay record, EngineScore/components, source prose and credit history do not reach Business or Auditor projections.

Evaluation holds Business/mandate authority, the source Auditor's identity and professional standing, and the current credit-fact version through the actual quote, application pointer, version and journal writes. The UUID binds the saved application revision and optional accepted principal. Each immutable encrypted quote contains the exact draft/version and optional accepted-principal input (including missing-evidence refusals), source verification ID/revision/hash/currentness, credit snapshot ID/revision/hash/provenance, mandate, actor/time, policy/calculation versions, calendar and deterministic engine record. PostgreSQL prevents quote update/delete and a composite foreign key prevents another application's quote from becoming the current pointer. Unexpected publication failure rolls back the entire effect and leaves no misleading receipt.

The quotation calendar uses `Africa/Kigali` months, with UTC evaluation instants stored separately. `UnderwritingObservationWindow` selects the latest complete audited 36-month baseline or qualified 12-month repeat window, excludes partial/future months and leaves gaps, duplicates and unverified selected months for the cash-flow gate to refuse. D-14 freshness is inclusive through the 45th Kigali calendar day after the selected last month's end. Older source observations remain immutable; selecting a window does not delete them or fill missing data. An unsigned Review spanning the first of the Kigali month must be requoted because the repayment schedule's start month changes, even when its statements remain fresh. Earlier signatories must accept the new quote; signatures bound to the old quote cannot complete the new agreement. The month change does not itself require newer statements, and submitted historical applications remain unchanged.

Quotes bind the selected calendar, window policy version and freshness expiry alongside the verification digest and exact selected engine inputs. Day 46 changes that binding and withdraws current signing authority, even within the same repayment month; a new evaluation records an explicit refusal. Quotes predating this binding require re-evaluation. Submitted snapshots and historical UUID outcomes remain unchanged. These are pre-issuance projections, not committed payment dates. Issuance must calculate again against its actual commitment/disbursement calendar and then pin the contractual schedule. Unknown history, withdrawn evidence and current restrictions produce explicit refusals without numeric offers. A lower accepted principal creates a new quote while retaining the original request and maximum offered amount, so it cannot bypass the original-request cutoff.

A normal draft save clears the current quote pointer. Reads also invalidate a quote when the effective mandate, evidence, source facts, policy/calculator or calendar changes. Historical UUID receipts remain immutable and can still report the old result under current actor authorization; they do not restore signing authority. Review advancement is a save of the unchanged evaluated draft and requires a current ready quote plus the descriptive fields. It retains the quote pointer while advancing the application revision. The signing and live transport contracts are recorded below.

Local quote/source evidence: 2,118 full non-TIA PHP tests / 10,730 assertions at 100% application lines; 85 PostgreSQL concurrency/worker tests / 320 assertions; zero PHPStan errors; five protected Business-model bypass probes rejected and clean architecture teardown; 940 frontend regressions; Vite+ checks pass with 40 existing warnings; generated inventory matches. These are local implementation gates, not S-A/S-B non-author review, hosted approval of this change or completion of checkpoint 2.

### Pinned application signatures and submission — checkpoint 2 S-A

`SubmitBusinessApplication` and `GetBusinessApplicationReview` use the existing `BusinessApplicationStore`. `BusinessApplicationSignature` and `BusinessApplicationSubmission` join the protected Business-model boundary: only `App\Infrastructure\Business`, model relationships and factories may access them. Both concrete models are negative-control targets. PostgreSQL rejects update/delete of either history, prevents cross-application quote references and prevents an application from pointing to another application's submission.

The signing action holds current Business/mandate authority, the source Auditor's identity/standing, credit facts, the application and a shared consent-catalog lock through the actual signature, final submission, version and journal writes. Publication takes the exclusive catalog lock. The current required Party is resolved from the verified active identity, not a client Party ID or typed name. The typed name is an encrypted attestation alongside actor/Party/time and the exact accepted versions. A Party has one signature per canonical agreement even across logins. A merely permitted representative who is outside the required set cannot substitute for a required signer.

The canonical agreement binds the immutable quote ID/revision/digest, full effective mandate and actual all-of signer set, complete legal release including original bodies/summaries/disclosures, policy version and the explicit CFG-01 zero listing-fee waiver. Quote and mandate changes or a new/withdrawn legal release cannot carry earlier signatures into a new agreement. The quote additionally validates its draft, evidence/credit versions, current source authority, policy/calculator and quotation month before signing. Missing or stale facts produce recorded refusals, with no fabricated clearance or legal text.

Partial signing appends an immutable signature and application version while preserving Review. The last required signature atomically creates the encrypted immutable submission and changes the application to submitted. The stored submission pins the accepted public snapshot and each constituent signature ID/digest. It is an application receipt: no Note is created, `note_id` is null and approval/publication remain pending. Historical submitted terms remain readable through current Business authority after later source/catalog changes; those historical terms are not a new borrowing authorization. Same-UUID retries and operation lookup retain their original outcomes; failed final writes roll back the signature, pointer/version and journal together.

The [early U-A handoff](https://github.com/rozine-rw/rozine/issues/96#issuecomment-5825729013) commits the shared route/Resource contract before transport publication. Erastus [started U-A](https://github.com/rozine-rw/rozine/issues/96#issuecomment-5825736628). The [autosave clarification](https://github.com/rozine-rw/rozine/issues/96#issuecomment-5825744192) makes an omitted `step` preserve the stored pointer, while explicit Continue requests are checked transitions. An edited autosave still invalidates its quote/signatures. A Back GET is an earlier rendered view only. Sole traders retain a null RDB company code, as [coordinated with U-A](https://github.com/rozine-rw/rozine/issues/96#issuecomment-5825798081). The live Resource/routes, isolated browser fixture and whole S-A/S-B acceptance remain separate work.

### Live application transports — checkpoint 2 S-A/S-E

`GetBusinessApplicationPage` and `ProjectBusinessApplicationOperation` extend the existing Business application port. Both transports use the same protected actions and explicit `BusinessApplicationResource`; API reads are `data`-wrapped and Inertia props are flat. Authenticated API reads require `business:read`; command responses require both `business:read` and `business:command`. Token abilities supplement current identity, mandate and record authority. Reads and receipts use private, no-store responses.

Create/resume returns a supplied `data.next` with opaque Business/application ULIDs. Save/evaluate/submit return the complete application/quote/acceptance/submission snapshot. The evaluate envelope echoes target, term and evidence version; these are compared to the locked saved draft and current verified evidence and never replace server financial facts. An earlier `view_step` GET cannot advance or persist the resume pointer. Autosave omits `step` and retains the saved pointer; edited fields invalidate the old quote.

The immutable operation outcome stays separate from current authority. Projection rechecks the current page and withholds command capabilities when the recorded application revision or review binding is superseded. A lookup completion must trigger a fresh page read rather than install an old receipt as current state. The submitted payload also pins its audience evidence projection, preserving the actual accepted period, financial totals and profile after later source changes.

`ApplicationEvidence` reports only verified observations with their actual rolling month range and per-calendar-year month count. Missing evidence/history remains null or an explicit refusal. Net operating cash is not accounting profit, statement verification is not an OCR claim, and audited/platform obligations do not imply CRB verification. The private EngineScore, credit provenance, account/source references and actor metadata are excluded from the public Resource. `home` remains null until its real financial facts are available; a separate minimal application entry list is being added to the existing Business role landing page. Full UI integration/browser acceptance and the completed S-A/S-B review remain outstanding.

### Authorized Business application entry points — checkpoint 2 S-A

`ListBusinessApplications` uses the Business application port, and `BusinessAuthorityStore::discover` supplies only a bounded page of candidate IDs. Discovery selects the actor’s current effective mandate membership under current Business identity; it acquires no Business row lock while holding the actor lock. Each candidate is then independently reauthorized through `WithBusinessAuthority` before current application metadata is projected. The final active-role check rejects an identity/context withdrawal during the read. Known per-record withdrawal omits the row while preserving the whole scanned page’s continuation; unexpected errors propagate. Both port and HTTP limits are 1–50, with a descending opaque-ID cursor.

The existing Business role landing page receives `business_applications`; other roles receive null. `/api/v1/business` exposes the same Resource with its own route prefix and token abilities. Only name, Business ID, latest draft/submitted ID/status/step/revision and supplied links/actions are returned. Current mandate permission controls create/resume; view-only members retain a read link. Read-only API tokens have no mutation actions. The Business entry page and API responses are private/no-store. The complete financial Home remains separate: this entry list supplies no synthetic wallet, capital or borrowing figures.

### Review corrections and terminal records — checkpoint 2 S-A/S-B

The [agreed review correction](https://github.com/rozine-rw/rozine/issues/96#issuecomment-5826579631) permits only one unresolved submitted application per Business in C2. Create, evaluate, Review advancement and submit check the existing submission under the Business authority lock before a new effect; old UUID receipts retain their historical outcomes. Home links the pending submission and withholds Create. A legacy draft may still be saved, but cannot be evaluated or signed while that submission is pending. Its authorized `pending_application` Resource contains only the application ID and supplied web/API read link. Canonical commitment/servicing accounting and resolution of that pending state remain later lifecycle work.

Signing rechecks the same descriptive fields used by Review advancement. An omitted-step autosave keeps its navigation pointer, but neither that pointer nor a newly evaluated numeric quote bypasses title/use-of-funds validation. Submit capability requires the Review step and these preconditions; a direct submit outside Review produces a recorded 422 `APPLICATION_STEP_INVALID`. A second unresolved application produces a recorded 409 `APPLICATION_PENDING_REVIEW`. Replayed refusals remain immutable after later correction or submission.

PostgreSQL denies update/delete of a submitted application, requires its status/step/current submission/current quote to agree, and enforces one submission per application plus one pending submission per Business. Closed Auditor assignments likewise reject update/delete. Both protection migrations take exclusive table locks during rollback and refuse removal once protected terminal history exists; changes to such history require an explicit forward migration. Unused protection can be reversed and reapplied without rewriting existing drafts, quotes or assignments. The Auditor read policy shares the dropped-record codes and caps Jobs/conflict pages at 50 in both port and HTTP paths. Read-lock optimization, explicit engagement binding and offer-time pagination remain open.

### Source-bound report foundation — checkpoint 2 S-D

`WithAuditApplicationBinding` is the private Business port used by the Auditor report adapter. It holds current accepted-assignment authority through the caller's effect and checks the immutable submitted application/version/submission/quote against their canonical hashes and source ownership. It returns only application, version, submission, quote and mandate identifiers, revisions and hashes, with the submitted timestamp. Original mandate terms, signer identities, credit facts, scorecard and private calculation inputs remain within the Business module. Later procedures must obtain any needed facts through an authorized, allowlisted source projection and verify their pins; an immutable report binding is not another copy of those private records. A later quotation month does not rewrite submitted history.

`StartAuditReport`, `GetAuditReport`, `GetAssignmentAuditReport` and `FindAuditReportOperation` enter through `AuditReportStore`. Starting is an explicit command with assignment revision and application ID/revision pins. Reads create no aggregate. The report and first version commit with the command journal while Business, actor, assignment and current professional-standing authority remain locked. Exact retries across logins for one Party retain the original minimal receipt; another UUID resumes the same report for that accepted assignment revision. A revoked/reassigned actor cannot recover private report content through an old receipt.

`AuditReportLifecycle` participates in the assignment adapter's already-authorized, locked conflict transaction. It withdraws the declarant's unfinished drafts and appends each withdrawal version with the conflict reference before dispatch changes. Any failure rolls back withdrawal, conflict declaration and reassignment together. A replacement Auditor's file has no current report until they explicitly start a new root, pinned to their Party and accepted assignment revision. The former binding, draft versions and any sealed history remain immutable; they are not copied into the replacement draft or presented as the replacement's findings. This rule also applies when Audit Operations later redispatches a case with no immediately eligible replacement.

`AuditReport` and `AuditReportVersion` are protected Auditor persistence targets, accessible only by that adapter namespace, model relationships and factories. Source pins and drafts are encrypted and omitted from model serialization. PostgreSQL enforces Business/assignment/application/version/submission/quote ownership, the exact current submitted application revision/quote/submission, the current accepted Auditor/assignment revision, one root per accepted assignment revision and one amendment per report within the same author/assignment revision. Source pins cannot change; draft revisions advance exactly once; terminal rows and every version are immutable. Transactional rollback refuses to destroy existing report history. An authorized read validates the current version and binding digests before projection. Private source facts are excluded from both report bindings and command receipts. Shared read locks and removal of repeated accepted-authority evaluation remain tracked optimizations. Report commands take the report row lock before the operation journal, after the already-held assignment row. This local exception cannot invert concurrent report commands: the assignment row serializes start, saves, ledger ingestion and conflict withdrawal before either competing journal/report lock can be taken. New report commands must preserve that order.

The report adapter also owns ordered procedure saves and ledger ingestion. `SaveAuditReportStep` locks current accepted-assignment authority and the report, checks the report revision and every retained source pin, appends the report version, and journals `audit.save_step` against `audit.report`. It never increments an assignment revision. Editing a completed step explicitly clears later observations, source pins and the saved note; rewinding before the ledger step also clears its active document references while immutable originals and earlier versions remain available; reading a previous step through `?step=` changes nothing and cannot open a future step. Monthly reports retain the cycle derived from the original dispatch, even if first opened later.

`IngestAuditLedger` uses the container-bound `AuditLedgerEvidence` port inside the report transaction. The protected `AuditLedgerOriginal` and `AuditLedgerExtraction` records belong to the report, independently of Business statement evidence. Encrypted PDF/UTF-8 CSV originals, pending extraction, report history and the immutable UUID receipt commit or roll back together. The scheduled extraction command processes committed ledger originals through the same bounded isolated parser, outside report/authority/journal locks; it appends extraction history and never changes the original. Re-scans may replace only an active original of this report, retain that reference in immutable report history, and never delete the earlier original. The receipt remains `INGESTED_NOT_AUDIT_APPROVED`; ingestion leaves the Business manifest, evidence revision and source verification unchanged and advances neither procedure nor approval. Saving the ledger step pins the active originals by ID, revision and hash; the preview distinguishes ledger originals from Business statements. Both transports offer report-scoped original downloads under current accepted-assignment authority, with private/no-store attachment responses and digest/integrity checks. PostgreSQL prevents evidence/history mutation and destructive rollback once records exist.

The ledger insertion guard independently locks its report and requires the original's Party to be that report's author, the report to be a draft Flash procedure at the ledger step, and the original revision to be the next report revision. A competing report advance cannot commit while that insertion is in progress. Existing ownership inconsistencies abort the forward migration without rewriting originals; rollback refuses to remove used protection. `LedgerStage.upload` advertises the authorized upload route only on the current editable ledger step, never a saved-step read, terminal report or read-only token. Retained originals and protected downloads remain visible on authorized back navigation. The procedure projection obtains MFA confirmation from the current account's identity facts rather than a constant; access still independently requires current MFA.

`GetAuditProcedureSources` composes the current factual statement verification with the assignment-scoped isolated source adapter while accepted authority is held. Unknown or review-required check-in facts, missing required photo metadata, more than five photos, absent declarations and stale pins block the appropriate step. Synthetic snapshot hashes are source-record hashes and never native image-attestation proof. `BuildAuditReportPreview` uses persisted observations and notes only; its canonical digest binds source versions, provenance, originals, findings version, procedure and licence. Query-string variance previews cannot enter that digest. The digest also pins current mandate, professional profile and engagement authority. Validation evidence and unfinished items remain in the Phase 1 checklist.

`DecideAuditReport` records request-changes or rejection only for a draft monthly filing. A permitted factual code and explanation are mandatory; missing or invalid choices produce recoverable recorded field errors. The terminal report version, encrypted reason and command receipt commit together while accepted authority and report locks remain held. Neither decision changes the submitted application or assignment revision, and the original report cannot be edited afterward.

Textarea explanations normalize CRLF/CR to LF, preserve ordinary newlines/tabs, and distinguish blank, excessive-length and unsupported-character errors. The journal keeps existing canonical request fingerprints unchanged. Previously unhashable inputs use a type-sensitive digest of the raw reason values so domain validation can retain a recorded 422 without leaking their text; old receipts continue to replay unchanged. Returned pages omit both the conflict permission and its action link. PostgreSQL separately restricts returned/rejected reports to monthly filings and requires every newly inserted amendment to begin as a draft at revision one and the first procedure step. Encrypted completion content remains subject to the application creation and version-integrity checks.

`AmendAuditReport` creates a fresh draft linked to a returned, rejected or sealed parent. It retains the parent's immutable source binding and period, records the parent's exact revision/version, and starts at the first procedure step without copied completion or ledger references. Current acceptance is rechecked. The parent remains unchanged; retries or another UUID resume its single child. PostgreSQL also requires a terminal parent with the same Business, author, accepted assignment revision, application version, submission, quote and report kind. The lineage migration refuses destructive rollback once amendments exist. Shared web/API Resources advertise decisions only for mutable monthly drafts and expose either a permitted amendment command or the existing child's link. Concurrent decisions and amendment creation serialize across logins belonging to the same Party.


### Auditor engagement retention — 2026-09-25

`AuditEngagementStore` owns compliance-released platform MSA and MVP-AUP-1 terms and explicit canonical-Party acceptance. `AuditEngagementRelease` and `AuditEngagementAcceptance` are protected Auditor persistence targets. The retained originals, approval reference, reason and acceptance payload are encrypted; updates/deletes are refused and rollback cannot discard history. Release revisions and version names are unique, and a composite foreign key pins each acceptance to the exact release ID/revision/hash. Input accepts full original document text, not a client-supplied hash or an implied acceptance. Synthetic terms remain unavailable outside isolated environments. Acceptance records the documents presented and accepted; it is not proof that an assignment's procedures, Business co-signature or seal are complete.

Publishing takes the exclusive engagement catalog advisory lock. Auditor acceptance and assignment authority hold the shared catalog lock through their protected transaction. Assignment lock order is Business, actor and ordered Parties, profiles, shared engagement catalog, locations/independence and assignment/journal. Current terms acceptance is an additional dispatch and offer-acceptance predicate and is required for protected evidence/report work. Receipt replay remains historical and never grants current authority. Replacing or withdrawing terms removes current eligibility without rewriting prior acceptance, source verification or report binding; new acceptance does not advance assignment revision or create another report. New protected effects receive the current acceptance pins while prior immutable facts retain their original attribution.

Report and statement-verification rows also retain `engagement_acceptance_id` as a database-visible projection of the encrypted original. Composite foreign keys bind the acceptance to the report author or source-verification actor. New acceptance/report/verification inserts require the latest active release; database triggers hold the shared engagement catalog lock through commit, and direct release insertion takes its exclusive counterpart. Published replacements therefore cannot slip between the currentness check and a source write. Existing report pins remain immutable after re-acceptance; statement verifications remain wholly immutable. Application reads additionally compare the relational acceptance ID with the encrypted pin.

The source-pin migration holds exclusive table/catalog locks and copies only these new metadata columns from verified canonical originals and matching retained acceptance rows. It never rewrites payloads, hashes, versions, timestamps or revisions, and it can retain a historical pin after its release was withdrawn. Temporary trigger suspension, metadata fill and constraints are one PostgreSQL transaction; unverifiable history aborts the entire migration. Rollback is allowed only before report or source history exists; used schemas require a forward migration. This is isolated test evidence, not permission to migrate an application database.

The shared web and `/api/v1` JSON boundary exposes `auditor-engagement-v1`: current full document bodies/hashes, minimal own acceptance, current actions and same-UUID lookup links. Reads create no acceptance. Commands use the standard OperationResource, current identity-context revision, release ID/revision/hash, strict boolean acceptance and UUID. The read Resource uses Laravel's `data` envelope; operation results retain their established top-level envelope. API reads/commands additionally require `auditor:read`/`auditor:command`, and responses are private/no-store. Auditor agreement UI integration is separate work owned by Erastus. Report/source bindings retain minimal agreement receipt pins, not duplicate document bodies or actor account details.

The [agreement UI handoff](https://github.com/rozine-rw/rozine/issues/96#issuecomment-5828936059) assigns the web Inertia rendering split, agreement page, banners and browser proof to Erastus. `GetAuditEngagementSummary` and its Resource supply only `{status, link}` on the live Auditor Home (`identity/role-home`), Profile, Jobs and file, including the file's background Jobs props. `current` means the current available release has this Party's retained acceptance; `required` means it is available but unaccepted; `unavailable` covers an absent, withdrawn, incompatible-procedure or environment-ineligible release. The GET link uses the matching web/API route. Other role homes return null. This summary is informational: controls still use fresh record-level `allowed_actions`, and protected commands independently reauthorize. Existing preview fixtures may carry null until their agreement scenarios are bound.

A retained release for another procedure is unavailable rather than corrupt. Its digest and document structure are checked before that compatibility decision, so actual tampering still fails integrity checks. Missing current terms remove new-dispatch and offer-acceptance eligibility but do not hide minimal existing Jobs/file records or prevent an authorized decline, conflict declaration, offer expiry or Operations case read. Private evidence/report work, including those modules' receipt lookups, still requires current acceptance; acceptance itself preserves the assignment revision and original report binding.

`auditor:engagement-terms {actor} {file} --expected-revision=... --request-id=... --no-interaction` publishes or withdraws through `RecordAuditEngagementTerms`, with the same current compliance permission, MFA configuration, environment checks, journal and catalog locks as any application caller. The local JSON file is limited to 5 MiB and explicitly supplies `status`, `version`, `documents`, `synthetic`, `approval_reference` and `reason`. Active releases supply both complete `{title, body}` documents; withdrawal supplies a null version and empty documents. The revision and UUID are mandatory, and an identical retry uses the same UUID. Success prints only the code, release ID, revision and digest; failures exit nonzero without printing document bodies or approval references. This provides a non-synthetic publication path in UAT/production but supplies neither approved legal content nor permission to promote or publish it. Every new active release currently requires fresh acceptance, with no grace period or silent non-material bypass; adding a non-material update policy remains separate work.

The current refusal contract is explicit while the code-name distinction remains a review follow-up:

| Situation | Outcome and recovery |
| --- | --- |
| JSON boolean `false` in the acceptance command | Recorded 422 `AUDIT_ENGAGEMENT_ACCEPTANCE_REQUIRED`, with `errors.accepted` and `field_errors.accepted`. Malformed booleans fail transport validation before journaling. |
| Protected evidence/report work or its receipt lookup without current acceptance | Unrecorded 403 `AUDIT_ENGAGEMENT_ACCEPTANCE_REQUIRED`; return to the agreement page, accept current terms, then fetch current facts and retry the original operation lookup. |
| Offer acceptance without current terms acceptance | Recorded 403 `AUDIT_ENGAGEMENT_ACCEPTANCE_REQUIRED`; the current offer omits `assignment.accept` but retains decline/conflict under their existing rules. |
| Acceptance while terms are absent, withdrawn or procedure-incompatible | Recorded 409 `AUDIT_ENGAGEMENT_TERMS_REQUIRED`, with null revision; refresh the agreement page, whose acceptance action is unavailable. |
| Acceptance against changed release pins | Recorded 409 `AUDIT_ENGAGEMENT_VERSION_CONFLICT`, with current release revision; refresh and explicitly accept the current originals. |

Historical engagement-acceptance receipts remain recoverable after release withdrawal and never grant current authority. The [review at `d24a6e95`](https://github.com/rozine-rw/rozine/pull/101#pullrequestreview-5315181399) was followed by the verified database-visible acceptance/author pins and acceptance/publication races described above. Shared read paths and remaining integration tests are planned with the procedure delivery; receipt-access behavior is agreed, and distinct refusal-code/input/metadata refinements stay on the non-blocking follow-up list. Those follow-ups and whole-checkpoint acceptance are not closed by the summary and publication-command corrections.

### Isolated assignment source facts — checkpoint 2 C2 acceptance

`RecordIsolatedAuditSourceFacts` is the trusted fixture publisher through `AuditSourceFactsStore` and `App\Infrastructure\Auditor\EloquentAuditSourceFactsStore`. It supplies the missing Business stock declaration and a synthetic capture-package summary to procedure tests only. It has no HTTP, web or gallery capture route, seeder or production activation, and a record is never a native/D-04 capture proof or a real Business assertion. As with credit facts, current `businesses.verify` staff/MFA authority and `EnvironmentIsolation::assertSeedingAllowed` are mandatory before publication or UUID replay, and an unknown assignment is only revealed after the permission check. Publication locks the Business, then staff authority, the journal and the assignment, which is the Business-first order every accepted Auditor read already takes.

Each append-only revision binds the exact accepted assignment ID and revision, the accepted Auditor Party, the Business and the current `MVP-AUP-1` procedure. The source revision sequence belongs to that binding and is separate from any report revision. A stale assignment revision is a recorded `ASSIGNMENT_VERSION_CONFLICT`, a stale source revision a recorded `VERSION_CONFLICT`, and an unaccepted assignment a recorded `ASSIGNMENT_NOT_ACCEPTED`. PostgreSQL refuses an insert that does not match the current accepted assignment row or does not advance the binding's revision by exactly one, and refuses every update or delete. Rollback is allowed only before source history exists. Facts, the `synthetic:` source reference and the reason are encrypted; the journal keeps only the request hash and the source identity/revision/status/digest receipt.

`GetAuditSourceFacts` reads through `WithAcceptedAuditAssignment`; `forAccepted` composes inside an already-held accepted-assignment operation and grants nothing alone. A replacement Auditor, a later assignment revision, revoked membership or a withdrawn engagement never inherits an earlier binding. The adapter verifies the canonical digest and stored binding before the availability decision. Missing, withdrawn, another-procedure and fixture-disabled sources are unavailable (`null`), and every declared fact inside an available source stays explicitly nullable, never defaulted to zero. Photo metadata is limited to bounded IDs, capture time, WGS84 position and title, with no image location. `AuditSourceSnapshot` joins the concrete Auditor-model architecture boundary and its negative-control loop.


### Auditor signing and Business publication boundary

`ConfirmAuditStepUp` obtains a current authenticator confirmation through the Identity-owned `Authenticator` port. Only a hash of the opaque proof is retained, bound to the account, canonical Party, identity context, report revision, digest and current authenticator credentials. The proof expires after five minutes and can be consumed once. Raw codes and proofs never enter the command journal or session error flashing. `SealAuditReport` repeats current accepted-assignment and source checks, signs the exact canonical preview, and commits proof consumption, immutable seal, terminal report version and pending publication atomically. A recovered UUID retains the original receipt under current authorization.

`AuditReportCryptography` owns the ES256 implementation behind its Auditor adapter. The current implementation is limited to isolated Alpha environments; encrypted P-256 private keys have a maximum 90-day signing interval, and retained public keys and immutable revocations preserve verification history. Key reads and revocations serialize through the actual write. A revoked key withdraws current seal validity without erasing the signed report. This adapter does not activate production key custody or native capture attestation.

`AuditReportPublicationStore` projects the sealed report to a currently authorized Business reader and accepts only the verified mandate's required Parties, each through a separately attributed immutable signature. Signatures bind the sealed revision, digest and mandate; publication occurs only when all required signatures and current Business, source, Auditor, engagement and key checks pass. Monthly publication uses the original reporting cycle through the full seventh Kigali calendar day; Flash reports have no invented monthly co-sign deadline. Pending N6 automatic-approval/dispute proposals are not active. Public verification exposes only opaque report identity, digest, seal validity and sealed amendment links.

The C2 amendment/publication boundary requires PostgreSQL **READ COMMITTED** for both the connection default and each transaction. The application uses this isolation, and `PostgreSqlConfigurationTest` asserts both settings. Amendment takes an exclusive parent-report lock; co-sign takes a shared parent lock before locking the publication, then checks for any child amendment. This relies on a fresh statement snapshot after the lock wait. A direct writer using REPEATABLE READ or SERIALIZABLE can retain a pre-amendment snapshot because creating the child does not update the parent. Those direct-write isolation modes are outside this boundary's current guarantee. Supporting them requires a separately tested durable withdrawal/conflict marker on a row that the signature path locks; changing the connection isolation is not a supported deployment adjustment. This defense-in-depth follow-up was identified in [the delivery-3 recheck](https://github.com/rozine-rw/rozine/pull/101#pullrequestreview-5319993028); it does not weaken the application's current-authority checks or authorize direct database publication.

`AuditStepUpProof`, `AuditSigningKey`, `AuditSigningKeyRevocation`, `AuditReportSeal`, `AuditReportPublication` and `AuditReportSignature` join the protected Auditor persistence boundary. PostgreSQL enforces actor-account provenance, seal/report-version atomicity, immutable signed history, distinct mandated signers and all-signatures publication; used signing/publication history refuses destructive rollback. Audiences receive projected Resources through Application actions, not model access.


### N6 monthly review amendment — contract agreed, implementation in progress (2026-09-26)

Robert's N6 decisions ([window and signatory](https://github.com/rozine-rw/rozine/issues/99#issuecomment-5832645053), [proof](https://github.com/rozine-rw/rozine/issues/99#issuecomment-5832727753), [dispute handling](https://github.com/rozine-rw/rozine/issues/99#issuecomment-5832878804)) authorize one current verified signatory, 24 hours from durable delivery to the Business app, and automatic approval on an undisputed timeout. Aminu authorized this amendment's implementation on 2026-09-26. Erastus confirmed the [server contract](https://github.com/rozine-rw/rozine/issues/96#issuecomment-5845469433) and [four clarified points](https://github.com/rozine-rw/rozine/issues/96#issuecomment-5845489284) in his [joint Engineering response](https://github.com/rozine-rw/rozine/issues/96#issuecomment-5845495647). This section records that contract, not a claim that it is active, tested, merged or deployed.

- New monthly seals pin `monthly-review-2026-09-26`, `delivered_at` in the sealing transaction and `due_at` exactly 24 hours later. Opening a page and retrying commands never restart the window. Legacy sealed history retains its previous publication rules; Flash keeps its current explicit-signature behavior. Each newly sealed monthly amendment opens its own window.
- Business `cosign` adds `policy_version`, `delivered_at`, `published_reason` (`signed`, `auto_approved`, `staff_resolved` or null) and a nullable dispute with `id`, `revision`, `status`, `submitted_at`, `outcome`, `resolution_note`, its retained `resolution_note_by` (`cpa`, `staff` or null) and a nullable amendment link. One signature completes the new monthly policy. System/staff approval never fabricates a human signature.
- `report.dispute` sends the existing context/publication revision/report revision/mandate/digest pins and UUID, plus optional plain `supporting_text` (1,000 characters) and optional `proof_files[]`; at least one is required. Files are limited to five PDF/JPEG/PNG originals of 10 MiB each, privately encrypted, server-hashed and served as authorized attachments. No additional mandatory reason prevents file-only proof. Idempotency binds normalized text and ordered original hashes/metadata, independent of multipart boundaries.
- A formal dispute freezes expiry immediately. The assigned CPA reads the proof and can start a linked amendment or use `audit.dispute.uphold` with an attributed reason (1,000 characters) to escalate to staff. Staff can explicitly escalate an unacted-on case; there is no invented CPA timeout or Business escalation button. `audit.dispute.resolve` either upholds/publishes or requires amendment. An escalated case stays frozen until staff resolution or an authorized amendment is sealed.
- `dispute.outcome` is null, `amendment_required`, `amended` or `upheld`. Requiring amendment stays escalated/frozen; the sealed replacement resolves the original with its link. Staff upholding publishes with `staff_resolved`. The Auditor sealed stage adds the authorized proof metadata/download links, resolution data and `actions.dispute_uphold`. Public seal verification retains its minimal five-fact boundary.
- A bounded scheduler performs timeout approval through the same Business/report/publication lock order and current Business, source, assignment, Auditor, engagement and key checks. Unavailable authority is a blocked case for Operations, never an approval. Reads never mutate publication. Human decisions require server time strictly before `due_at`; the scheduler becomes eligible at that boundary. Concurrent signature, dispute, timeout and amendment attempts must have one consistent outcome.
- The existing `audit.assignments.manage` capability governs staff dispute operations, with current MFA and immutable attributable reasons. Transport Resources supply matching web/API routes and allowed actions; clients key new copy on the policy version. C5 preparation notifications and wider servicing are separate deliverables.

Aminu owns the persistence/application/API/scheduler implementation and PostgreSQL race evidence. Erastus owns Business and Auditor UI integration and responsive/browser proof. Promotion waits for the integrated exact-commit gates and non-author review; the legacy signing/publication boundary above remains the implemented baseline until then.

The N6 review correction separates an objection from publication authority: a current required Business signer can dispute within the retained window even while CPA standing or source currency is unavailable. Its mandate, seal integrity, amendment absence and deadline are still checked. A staff `amendment_required` decision cannot be reversed by another resolve command; the Resource withdraws that action and the event trigger rejects another staff resolution. Event hashes bind publication/revision, command, account/Party attribution, decision time, payload and the preceding hash; reads verify the complete retained chain. Proof listings select only metadata. Operations case review entries expose due time and current publication availability/refusal so blocked expiry can be identified without fetching originals.

The decision clock remains the application's captured UTC instant. PostgreSQL validates that supplied instant against the immutable deadline; it does not independently attest the clock of a privileged direct SQL writer. Statement-level `TRUNCATE` guards across the existing audit-history tables remain a separate defense-in-depth follow-up; production runtime roles must not have schema ownership or TRUNCATE privileges.

Before enabling the five-file dispute envelope in a hosted environment, verify the actual PHP-FPM pool and ingress settings: `upload_max_filesize = 10M`, `post_max_size = 64M`, `max_file_uploads >= 5`, nginx `client_max_body_size 64m` (or equivalent), and a memory limit/load test sufficient for ingesting five 10 MiB originals (start at `256M`). A 50 MiB multipart smoke test through the real ingress must pass. These values are deployment prerequisites, not verified staging/production settings. Original bytes are read only for ingestion or a specifically authorized download; ordinary report, decision and Operations reads use metadata. A failed proof-integrity check is a fail-closed 503.


### C3 acceptance exposure foundation — 2026-09-26

`BusinessExposureStore` is the internal source port for current accepted exposure and reservation from a retained submission. It is container-bound to `EloquentBusinessExposureReservations` and reached by the existing authorized `BusinessApplicationStore` commands; persistence models never cross the port. `BusinessExposureReservation` joins the protected Business model set and its direct-write negative control. No new actor-facing command or acceptance checkbox is added.

Only the final required `application.submit` signature writes the reservation, in the same transaction as immutable submission, application revision and operation receipt. The economic commitment ID equals the retained submission ID; the shared submission projection exposes it as optional `exposure_reservation_id` (absent for pre-C3 history). Unique application/submission keys and composite parent foreign keys prevent a second reservation or a cross-Business binding. PostgreSQL serializes inserts on the Business aggregate and prohibits update/delete; occupied reservations require a forward migration to add release/issue transitions. Historical submissions are not backfilled.

Underwriting version `application-underwriting-2` includes accepted commitments in its private replay inputs. Exact principal is counted once by economic ID across source obligations and reservations; conflicts fail closed. An unissued commitment does not manufacture a repayment calendar or future interest exposure. Quotes pin the current reservation set, and final acceptance checks it while current source, authority and Business locks are held. Old draft quotes require reevaluation; retained submitted terms and receipts stay unchanged.

This foundation does not enable staff release, campaign publication, ledger funding or provider payment. Those follow in the ordered C3 slices and consume the retained commitment; there is no production credit-source override or historical acceptance migration in this change.
